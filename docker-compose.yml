services:
  migrate:
    build:
      context: .
      target: migrator
    pull_policy: build
    environment:
      - DATABASE_URL=postgresql://taicca:taicca_pass@db:5432/taicca_onboarding
    depends_on:
      db:
        condition: service_healthy
    restart: "no"

  app:
    build:
      context: .
      target: runner
    pull_policy: build
    ports:
      - "3001:3000"
    environment:
      - DATABASE_URL=postgresql://taicca:taicca_pass@db:5432/taicca_onboarding
      - NEXTAUTH_URL=http://metalpig.synology.me:3001
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - HR_ALLOWED_EMAILS=${HR_ALLOWED_EMAILS}
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - SMTP_FROM=${SMTP_FROM}
      - DEPARTMENTS_API_URL=http://host.docker.internal:3300
      - DEPARTMENTS_API_KEY=${DEPARTMENTS_API_KEY}
      - RAGIC_API_URL=${RAGIC_API_URL}
      - RAGIC_API_KEY=${RAGIC_API_KEY}
      - UPLOAD_DIR=/uploads
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - uploads:/uploads
    depends_on:
      migrate:
        condition: service_completed_successfully
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: taicca_onboarding
      POSTGRES_USER: taicca
      POSTGRES_PASSWORD: taicca_pass
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U taicca -d taicca_onboarding"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  pgdata:
  uploads:
